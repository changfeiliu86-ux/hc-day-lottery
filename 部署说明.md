# 📝 GitHub Pages 部署详细步骤

## 第一步：在 GitHub 上创建仓库

1. **打开浏览器，访问**：https://github.com/new

2. **填写仓库信息**：
   - **Repository name**（仓库名）：例如 `hc-day-lottery` 或 `lottery-system`
   - **Description**（描述）：可选，例如 "HC Day 抽奖系统"
   - **选择 Public**（公开）✅
   - **不要勾选** "Add a README file" ❌
   - **不要勾选** "Add .gitignore" ❌
   - **不要勾选** "Choose a license" ❌

3. **点击绿色的 "Create repository" 按钮**

4. **记录你的信息**：
   - 你的 GitHub 用户名（例如：`liuchangfei`）
   - 你创建的仓库名（例如：`hc-day-lottery`）

---

## 第二步：在终端运行命令

### 方法一：使用部署脚本（推荐）

1. **打开终端**（Terminal）

2. **运行以下命令**（替换成你的实际信息）：

```bash
cd /Users/liuchangfei/HC-Day
./deploy.sh 你的GitHub用户名 你的仓库名
```

**示例**：
- 如果你的 GitHub 用户名是 `liuchangfei`
- 仓库名是 `hc-day-lottery`
- 那么命令就是：

```bash
cd /Users/liuchangfei/HC-Day
./deploy.sh liuchangfei hc-day-lottery
```

### 方法二：手动执行命令

如果你不想用脚本，可以手动执行：

```bash
cd /Users/liuchangfei/HC-Day
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

**示例**：
```bash
cd /Users/liuchangfei/HC-Day
git remote add origin https://github.com/liuchangfei/hc-day-lottery.git
git push -u origin main
```

---

## 第三步：启用 GitHub Pages

1. **在 GitHub 仓库页面**，点击顶部的 **"Settings"**（设置）

2. **左侧菜单**找到 **"Pages"**（页面）

3. **在 "Source" 部分**：
   - 下拉菜单选择 **"main"** 分支
   - 文件夹选择 **"/ (root)"**
   - 点击 **"Save"**（保存）

4. **等待 1-2 分钟**，GitHub 会生成你的网站

---

## 第四步：访问你的网站

几分钟后，你的网站就可以通过以下地址访问：

```
https://你的用户名.github.io/你的仓库名/
```

**示例**：
- 用户名：`liuchangfei`
- 仓库名：`hc-day-lottery`
- 访问地址：`https://liuchangfei.github.io/hc-day-lottery/`

---

## 常见问题

### Q: 如果提示需要登录 GitHub？
A: 需要配置 GitHub 认证。可以：
- 使用 GitHub CLI：`gh auth login`
- 或使用 Personal Access Token

### Q: 如果提示 "repository not found"？
A: 检查：
1. 仓库名是否正确
2. 仓库是否已创建
3. 仓库是否是 Public（公开）

### Q: 如果推送失败？
A: 可能需要先拉取远程仓库：
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

---

## 完成！

部署成功后，你就可以将网站链接分享给任何人了！🎉

