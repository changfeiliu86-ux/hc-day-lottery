# 🚀 快速部署步骤

## 第一步：创建 GitHub 仓库

1. 访问：https://github.com/new
2. 仓库名：`hc-day-lottery`（或其他名称）
3. 选择 **Public**
4. **不要**勾选任何初始化选项
5. 点击 "Create repository"

## 第二步：创建 Personal Access Token

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. Note：填写 `HC Day Lottery`
4. 勾选权限：**`repo`**（完整仓库权限）
5. 点击 "Generate token"
6. **重要**：复制生成的 token（类似：`ghp_xxxxxxxxxxxxx`），只显示一次！

## 第三步：推送代码

在终端运行：

```bash
cd /Users/liuchangfei/HC-Day
git push -u origin main
```

当提示输入时：
- **Username**：输入 `changfeiliu86-ux`
- **Password**：**粘贴你的 Personal Access Token**（不是 GitHub 密码！）

## 第四步：启用 GitHub Pages

1. 在 GitHub 仓库页面，点击 "Settings"
2. 左侧找到 "Pages"
3. Source 选择 "main" 分支
4. 点击 "Save"

## 第五步：访问网站

几分钟后访问：
`https://changfeiliu86-ux.github.io/hc-day-lottery/`

---

## 或者：手动安装 GitHub CLI

如果你想用 GitHub CLI，可以手动安装：

1. **安装 Homebrew**（如果还没有）：
   ```bash
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   ```
   需要输入管理员密码

2. **安装 GitHub CLI**：
   ```bash
   brew install gh
   ```

3. **登录**：
   ```bash
   gh auth login
   ```
   选择 GitHub.com，HTTPS，然后按提示操作

4. **推送代码**：
   ```bash
   cd /Users/liuchangfei/HC-Day
   git push -u origin main
   ```

